--TEST--
Fiber getCurrent static method
--FILE--
<?php
echo "Outside fiber: ";
$current = Fiber::getCurrent();
echo ($current === null ? "null" : "not null") . "\n";

function testCurrent() {
    echo "Inside fiber: ";
    $current = Fiber::getCurrent();
    echo ($current === null ? "null" : "not null") . "\n";
}

$fiber = new Fiber('testCurrent');
$fiber->start();
--SKIPIF--
Instance methods not fully implemented yet

--EXPECT--
Outside fiber: null
Inside fiber: not null